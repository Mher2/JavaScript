// 1. Declare 2 variables a and b, such that a > b, and define values for them. Output their

let first_number = 14;
let second_number = 5;

const sum = first_number + second_number;
const difference = first_number - second_number;
const product = first_number * second_number;
const quotient = first_number / second_number;
const remainder = first_number % second_number;

console.log(`sum = ${first_number} + ${second_number} = ${sum}`);
console.log(`difference = ${first_number} - ${second_number} = ${difference}`);
console.log(`product = ${first_number} * ${second_number} = ${product}`);
console.log(`quotient = ${first_number} / ${second_number} = ${quotient}`);
console.log(`remainder = ${first_number} % ${second_number} = ${remainder}`);
